{% extends 'layout.html.twig' %}

{% block content %}
   <section>
        <div class="container ajout-news">
             <div class="row">
                <div class="col-12 col-md-12">
                    
                </div>
            </div>

            {{ form_start(form) }}
            {{form_row(form.titre)}}
            {{form_row(form.contenu)}}
            {{form_row(form.image) }}
            {{form_errors(form.titre)}}
            {{form_errors(form.contenu)}}
            {{form_errors(form.image) }}
            
   
             <button type="submit" class="btn btn-primary">Modifier votre actualité</button>
            {{ form_end(form) }}
        </div>
    </section>
    {% endblock %}